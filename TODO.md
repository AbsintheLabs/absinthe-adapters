## Todo List

[ ] Remove legacy adapter code
[ ] Dockerfiles for each adapter (remove `ops` folder)
[ ] Remove `assets` folder
[ ] Remove `manifest_test_cases` folder
[ ] Remove `packages/common` folder - we don't depend on this anymore
[ ] Remove `oldsrc` folder (this is also deprecated)
[ ] Remove `src/abi` folder and `src/adapters` folder -> this needs to get ported as a proper adapter instead of being at the source
